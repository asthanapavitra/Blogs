
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Blog</title>

    <style>
        body{
            
            font-family:'Times New Roman', Times, serif;
            height: 100%;
            margin: 0;
            display: flex;
            justify-content: center;
            align-items: center;
           /* background-color: black; */
        }
    #container {
        margin-top: 50px;
        width: 800px; /* Adjust width as needed */
        padding: 20px;
        box-shadow: 0 4px 8px 0 rgba(156, 154, 154, 0.2); 
        border-radius: 10px;
    }
    .ele {
        padding: 10px;
        width: 100%;
         /* Full width elements */
    }
    .form {
        width: 100%; /* Full width of the container */
        background-color: white; /* White background for the form */
        padding: 20px; /* Padding inside the form */
        box-sizing: border-box; /* Include padding in the width calculation */
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
    }
    .content {
        height: 200px;
        width: 100%;
    }
    .text, button {
        padding: 5px;
        border-radius: 10px;
        border: 2px solid gray;
        width: 100%; /* Full width inputs and button */
        font-size:1.25rem ;
    }
    button {
        background-color: rgb(56, 56, 222);
        color: white; /* Text color for better readability */
    }
   
    .title {
        height: 40px;
    }

    /* Media query for tablets */
    @media (min-width: 768px) {
        .container {
            flex-direction: row; /* Side by side elements on larger screens */
            flex-wrap: wrap; /* Wrap elements if needed */
            width: 100%; /* Use the full screen width */
            margin-top: 50px;
            padding: 0 10px; /* Add some padding on the sides */
        }
        
        .text, button {
            padding: 5px;
            border-radius: 10px;
            border: 2px solid gray;
            width: 100%; /* Adjust width for text inputs and button */
        }
        button {
            background-color: rgb(56, 56, 222);
            color: white;
            width: 1;
        }
       
        .title {
            height: 40px;
            width: 100% ; /* Adjust width for .title */
        }
    }

    /* Media query for desktops */
    @media (min-width: 992px) {
        .container {
            max-width: 100%; /* Max container width */
            margin: 50px auto; /* Center container */
        }
        .ele {
            width: 500px; /* One third width elements with margin */
        }
        .content {
            width: 500px;
            height: 300px; /* Two thirds width textarea */
        }
    }
        
    </style>
</head>
<body>
    <div id="container">
    <form action="/update/<%= post.id %>" method="POST">
        <div class="form">
            <div><h1>Edit Your Blog</h1></div>
            <div class="ele">
          
            <input class="text title" type="text" name="title" value="<%= post.title %>" required><br></div>
            <div class="ele">
          
            <textarea class="text content" name="content" required><%= post.content %></textarea><br></div>

            <div class="ele"><button type="submit">Save Changes</button><br></div>
        </div>
    </form>
    </div>
</body>
</html>

